import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'admin-frontend-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
