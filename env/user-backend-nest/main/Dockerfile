FROM node:${NODE_VERSION}

ENV API_GATEWAY_SERVER_PORT=3000
ENV API_GATEWAY_CORS_ORIGIN=http://localhost:8100

ENV NODE_ENV=production

#ENV INFO_SERVICE_PORT=3003
#ENV INFO_SERVICE_HOST=localhost

#ENV AUTH_SERVICE_PORT=3004
#ENV AUTH_SERVICE_HOST=localhost

COPY dev/user-backend-nest/main/dist/ /usr/app/

WORKDIR /usr/app
