apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-multi-pwa-service
  labels:
    {{- include "pwa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pwa.selectorLabels" . | nindent 4 }}
