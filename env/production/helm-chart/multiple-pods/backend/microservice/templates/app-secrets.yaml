apiVersion: v1
kind: Secret
metadata:
  name: multi-backend-secrets-{{ .Values.appService.name }}
type: Opaque
data:
  {{- if and .Values.appService.env .Values.appService.env.secret }}
  {{- range $key, $val := .Values.microService.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end }}
  {{- end }}